# 复制此文件为 .env，填写真实值后部署
# Railway 上在 Variables 面板设置这些环境变量

# 必改：JWT 签名密钥（随机长字符串）
SECRET_KEY=change-me-to-a-random-long-string-32chars

# 必改：管理后台登录密码
ADMIN_PASSWORD=your-strong-admin-password

# 可选：token 有效期（天），默认 30
TOKEN_EXPIRE_DAYS=30

# 可选：数据库文件路径（Railway 挂载 Volume 时配置）
DB_PATH=users.db
